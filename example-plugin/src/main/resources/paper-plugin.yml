name: CherryDemo
version: '1.0.0'
main: com.example.cherrydemo.CherryDemoPlugin
api-version: '1.20'
author: Cherry
description: >-
  Example Cherry plugin - proves cherry-gradle-plugin end to end. A Cherry plugin is still, first,
  a completely normal Paper plugin: this file is untouched by cherry-gradle-plugin, which only adds
  cherry-plugin.json plus the generated mixin config/refmap alongside it.
